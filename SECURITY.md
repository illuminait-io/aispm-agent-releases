# Security

Report vulnerabilities in the AISPM agent or in these packages privately to
security@illuminait.io — do not open a public issue.

Every release asset is accompanied by `SHA256SUMS` and a `manifest.json`
carrying per-asset sha256 checksums; the install scripts verify them
automatically. If a checksum mismatch is ever observed, stop and report it.
